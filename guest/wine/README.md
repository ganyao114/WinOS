# Wine Shared Assets

`guest/wine/include` is synchronized from Wine's `include/` tree and shared by
all guest DLL ports.

Use `./scripts/sync_wine_sources.sh` to refresh:

```bash
./scripts/sync_wine_sources.sh /path/to/wine-source
```
