include ../common.mk

OUT := $(SYSROOT)/syscall_section_open_test.exe

$(OUT): syscall_section_open_test.c | $(SYSROOT)
	$(CC) $(CFLAGS) -nostartfiles -o $@ $< \
		-L$(SYSROOT) -lntdll \
		-Wl,-e,mainCRTStartup

.PHONY: clean
clean:
	rm -f $(OUT)
