[package]
name = "winereg"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
thiserror = "1"
once_cell = "1.21"
regex = "1.12"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
