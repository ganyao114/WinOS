[package]
name = "winereg"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = ["dep:chrono"]

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock"], optional = true }
